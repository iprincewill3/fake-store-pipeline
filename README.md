# Fake Store ETL Pipeline

This project extracts product data from the Fake Store API, transforms it in Python, 
and saves curated data as Parquet and CSV for analysis in Power BI.

## How to run locally

```bash
pip install -r requirements.txt
python flows.py
